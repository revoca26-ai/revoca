{
  "name": "revoca",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\""
  },
  "devDependencies": {
    "@types/node-cron": "^3.0.11",
    "concurrently": "^9.1.2"
  },
  "dependencies": {
    "node-cron": "^4.6.0",
    "openai": "^6.45.0",
    "zod": "^4.4.3"
  }
}
